# Local AWS deployment credentials. Copy this file to `.env` and fill in the
# values locally. The real `.env` is ignored by Git and Docker build context.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
AWS_REGION=us-east-1
