---
title: 7. 详细接口文档
description: >-
  为减少重复描述，增加信息密度，以下接口文档中有如下约定： 1. HTTP JSON RPC 中的请求和回复消息的 header
  字段不进行详细解释，示例消息中有些 header 被省略，实际请求时也可不填，以实际可运行示例为准； 2. HTTP JSON RPC 中的 URL IP
  均填写为 127.0.0.1，实际访问时需要改写为能够访问到的 x86 工控机 IP； 3. HTTP JSON RPC 的所有请求方法均为
  POST，不在…
product: a2-lite
version: v2-0
slug:
  - a2-lite
  - v2-0
  - detailed-interface
route: /zh/a2-lite/v2-0/detailed-interface
sourceUrl: 'https://open.agibot.com/docs/aimdk/a2-lite/v2_0/07-00-detailed_interface'
order: 75
---
# 7. 详细接口文档

为减少重复描述，增加信息密度，以下接口文档中有如下约定：

1.  HTTP JSON RPC 中的请求和回复消息的 header 字段不进行详细解释，示例消息中有些 header 被省略，实际请求时也可不填，以实际可运行示例为准；
2.  HTTP JSON RPC 中的 URL IP 均填写为 127.0.0.1，实际访问时需要改写为能够访问到的 x86 工控机 IP；
3.  HTTP JSON RPC 的所有请求方法均为 POST，不在下面接口文档中单独列出；
4.  ROS2 Topic 中消息类型一列若为入参，则由二开程序发布该话题，机上程序接受该话题，若为出参则相反；
5.  未解释参数一般即为无效参数，按照示例填写默认值不做修改即可；
6.  未注明单位的，角度默认使用 rad，长度默认使用 m；

